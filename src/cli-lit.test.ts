import { cliLit } from './cli-lit';

describe('test cli lit', () => {
  it.todo('should reflect the correct version');
  it.todo('should define functions without any decorators');
  it.todo('should replace function name if cliName decorator exists');
});
